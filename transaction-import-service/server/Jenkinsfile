pipeline {
    agent any

    stages {
        stage('Clone repository') {
            checkout scm
        }

        stage('Build') {
            steps {
                sh 'mvn -DskipTests clean package'
            }
        }

        stage('Test') {
            steps {
                sh 'mvn test'
            }
        }
    }
}