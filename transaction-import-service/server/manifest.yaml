apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: transaction-import-service
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: fima
        tier: backend
        track: stable
        name: transaction-import-service
    spec:
      containers:
        - name: transaction-import-service
          image: "$NEXUS_HOST:8123/transaction-import-service:$BUILD_ID"
          ports:
            - name: http
              containerPort: 9997
      imagePullSecrets:
        - name: regcred
---
kind: Service
apiVersion: v1
metadata:
  name: transaction-import-service
spec:
  selector:
    app: fima
    tier: backend
    name: transaction-import-service
  ports:
    - protocol: TCP
      port: 9997
      targetPort: http